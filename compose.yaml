services:
  ai-qcell:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - ./artifacts:/app/artifacts
    environment:
      STREAMLIT_SERVER_MAX_UPLOAD_SIZE: "500"
    restart: unless-stopped
