services:
  ai-qcell:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - ./artifacts:/app/artifacts
    environment:
      PORT: "8501"
      QCELL_AUTH_MODE: "demo"
      STREAMLIT_SERVER_MAX_UPLOAD_SIZE: "500"
    restart: unless-stopped
